{{ form_start(eventForm) }}
    {{ form_row(eventForm.eventName) }}
    {{ form_row(eventForm.description) }}
    {{ form_row(eventForm.place) }}
    {{ form_row(eventForm.beginDate) }}
    {{ form_row(eventForm.endDate) }}
    {{ form_row(eventForm.type_event) }}
    {{ form_row(eventForm.image) }}
    <button type="submit" class="btn btn-lg btn-primary">Soumettre</button>
{{ form_end(eventForm) }}           